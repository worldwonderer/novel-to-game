# Project Plateau progress

- source: Arthur Conan Doyle, *The Lost World*
- mode: quick
- current stage: done
- targetFinish: playable-prototype
- assuranceProfile: release
- current candidate proof: `qa/verification.json`
- current release proof: `qa/release-gates.json`

## Stage handoffs

- `gate:intake PASS` — product, runtime, audience and rights boundaries are locked.
- `gate:analyze PASS` — all 16 chapters are covered in the source bible.
- `gate:concept PASS` — the field-record survival direction is selected.
- `gate:design PASS` — the 1–3 minute expedition loop, outcomes and restart are frozen.
- `gate:art PASS` — the approved field-photography visual direction and targets are frozen.
- `gate:build PASS` — the browser candidate builds and the complete route is runnable.
- `gate:qa PASS` — the current verification and release closure pass repository validation.

## Public gates

- `gate:scope PASS` — approved product, source, concept, game design and art direction agree.
- `gate:playable PASS` — one real-input route reaches a Strong result and restarts cleanly.
- `gate:claim PASS` — playable-prototype claims bind the current visual manifest; the public-host
  record remains explicitly historical.

## Retention note

The old S0–S10 iteration families were reproducible development history, not current release roots. The repository
keeps one semantic `build/evidence/current-run/` closure, the curated visual manifest and frozen reviews. Detailed
history remains available in Git rather than in the working tree.
